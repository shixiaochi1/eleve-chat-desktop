fix(chat): 复核修正两处——完成卡片文本截断保留 + HistoryCursor 归一工厂

独立复核（847d5bb + 6677088）发现的两个问题：

1. delegateTasks 完成态剥离丢失显示（功能退化）：SubagentMonitor 完成卡片
   消费 thinkingText/lastText 渲染"思考：/回复："行（无 isDone 守卫），
   原整字段剥离 = 完成后两行消失。修正：outputTail/toolArgs/files* 仍剥离
  （无消费方），thinkingText/lastText 改为 cap 200 字符截断保留
  （显示 truncate 一行 + title 悬停预览，200 字符足够）。

2. HistoryCursor 手写 standalone atom 与 store-factory 并行（重复模式）：
   新 store 应走 createAtomStore 工厂（isStreaming 是工厂诞生前的既有例外
   勿效仿）。迁移后删除手写 listeners/useCallback subscribe（-6 行样板）。

复核其余 14 项 PASS（storage/saveBeacon 链、msgCache LRU 写点全覆盖、
debug 三值合并订阅粒度等价、releaseAgentTerminal 挂钩无双重处理、
分层归位零残留、分页竞态防护闭环）；遗留记录：useArtifactsGallery/
ArtifactPanel 的全量 get_session_messages 是产物扫描合法用例（传 limit
会丢老消息产物），不动。

验证：tsc --noEmit 0 错误；vitest 86/86；vite build 成功
