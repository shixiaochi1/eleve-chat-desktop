# ELEVE 前端审查 Phase 3-8 — 补齐审查报告

**日期**: 2026-08-01
**基线**: main @ 346ced1
**方法**: 5 个只读子 Agent 并行（grep+read 实证）+ 主 Agent 亲自核查关键 P1（codegraph/grep）
**说明**: 上个会话 Phase 3-8 仅一行结论带过，本次按审查文档逐项实证补审。主 Agent 亲自验证项标注 ✅。

---

## 总览

| Phase | 区域 | P1 | P2 | P3 | 主 Agent 核查 |
|-------|------|----|----|----|----|
| 3 | 性能与渲染 | 0 | 6 | 5 | App.tsx:84 ✅ |
| 4 | 终端与预览 | 5 | 2+ | 3+ | PreviewPanel sandbox ✅ |
| 5 | 设置与 Provider | 4 | 2 | 1 | 字段错配 ✅ |
| 6 | Kanban 与辅助面板 | 2 | 7 | 2+ | api.ts 前缀 ✅ |
| 7+8 | 语音/主题/基础设施 | 0 | 5 | 10+ | — |
| **合计** | | **11** | **22** | **21+** | |

**重要前提修正（实证推翻原审查假设）**：
- 前端无 zustand（自研 useSyncExternalStore 原子 store）
- 前端无 MediaRecorder（录音后端驱动，useVoice 是纯状态机，UI 禁用态）
- storage.ts 是 IPC 文件存储（非 localStorage 封装）
- 主 IPC 通道是 WS JSON-RPC（非 Tauri invoke，仅 4 个原生 invoke）

---

## Phase 3: 性能与渲染质量

### P2（6 项）
| # | 文件:行 | 问题 | 建议 | 核查 |
|---|---------|------|------|------|
| 3-1 | App.tsx:84 | 根组件 `useMessages()` 订阅全量数组，流式 ~30fps 重渲染整棵根树；实际只用 `.length`（L985/1026），透传的 messages 默认面板不消费 | 改派生订阅 `useMessageCount`/复用 signature，面板自行订阅 | ✅ 已验证 |
| 3-2 | App.tsx:1036 | 内联 `onOpenSettings={()=>...}` 击穿 `memo(VirtualizedThread)`，MessageContainer 30fps 重渲染 | 提为 useCallback | |
| 3-3 | App.tsx:924-932 | ModelProvider value 内联对象（含内联箭头），每帧新引用 → 所有 context 消费者 30fps 重渲染 | useMemo 包裹 value | |
| 3-4 | InputArea.tsx（整体 memo=0） | 594 行大组件未 memo，流式 30fps 重渲染，props 实际稳定 | 包 memo(InputArea) | |
| 3-5 | AgentChatCard.tsx:311 | 宫格卡片消息列表未虚拟化，N×最多100 条 MessageRow 全挂载 | 卡片内引入 @tanstack/react-virtual（已是依赖） | |
| 3-6 | App.tsx:263/266 + useMessageStream(onToolArgs/onDelegateProgress/onThinking/onStatusUpdate) | 每网关事件触发 App 级 setState（debugEvents/monitorState/debugToolCalls），叠在 30fps flush 上放大根重渲染 | debug/monitor 状态下沉独立 store 或面板自订阅 | |

### P3（5 项）
- ToolEntry memo 恒失效（MessageRow:57 每次新建 tool 字面量）
- ReasoningBlock/QueuePanel/MessageBubble 无 memo
- OutlinePanel:172/TerminalPanel:40 自行 useMessages 打开时 30fps + O(n) 重算
- chrome 组件（AppShell/PaneShell/IconBar/StatusBar）未 memo（修 3-1 后缓解）
- react-virtuoso 声明但零使用（死依赖）；useScrolledUp 死代码

**亮点**：flush 链路本身高效（queueDelta 节流 33ms + 合并 mutateStream + signature O(1) 快通道 + per-index useMessage）；宫格跨卡隔离生效（未变卡 bail out，无串扰）。问题在消费端广度，不在 flush。

---

## Phase 4: 终端与预览

### P1（5 项）
| # | 文件:行 | 问题 | 触发条件 | 核查 |
|---|---------|------|----------|------|
| 4-1 | TerminalPanel.tsx:51,257; useTerminal.ts:24-27,86 | 多终端隔离未实现：所有 tab 共享单一 xterm 实例，切 tab/新建内容不变 | 创建≥2 tab 或切换 | |
| 4-2 | useTerminal.ts:88-90（空依赖 useCallback）; terminal-buffer.ts:48-51 | reader 注册捕获首渲染 id 陈旧闭包，切 tab 后 readActiveTerminal 返回 null，agent read_terminal 得空串 | 切 tab 后 agent 读终端 | |
| 4-3 | TerminalPanel.tsx:131-145 | 流式场景 callId 预标记"已写"，tool 结果到达后永不补写 | 面板已挂载时 agent 流式执行 terminal | |
| 4-4 | PreviewPanel.tsx:222,219 | iframe `allow-scripts+allow-same-origin` 可逃逸 sandbox；`src={url.trim()}` 零协议过滤/白名单 | 加载同源内容（或 CSP 放宽后任意页） | ✅ 已验证 |
| 4-5 | terminals.ts:166,180（零调用）; terminals.ts:227-233 | agent 只读镜像链路只有 close 侧：ensureAgentTerminal/openAgentTerminal 无调用方、无 terminal.output 路由，terminal.close 恒 no-op | agent 执行 terminal(background=true) | |

### P2（2+ 项）
- useTerminal.ts:24-96 异步 init 竞态：import 解析前多次 init → 双 Terminal 创建（首个孤儿化）；卸载先于注册 → readers Map 永久持孤儿
- 条件渲染卸载：侧栏不可见时 reader Map 空 → 读取空串
- reviveBuffer 死特性（写端零调用，重启后内容全丢，文档"跨重启重放"不实）

**说明**：桌面端无真实 PTY（后端 terminal.resize 是空实现），resize 不传后端属设计使然（P3）；terminal.output 事件双侧不存在。

---

## Phase 5: 设置与 Provider UI

### P1（4 项）
| # | 文件:行 | 问题 | 根因 | 核查 |
|---|---------|------|------|------|
| 5-1 | MCPSettings.tsx:123-135 | MCP server 删除重启后复活 | config.set.raw 加性合并，省略 key≠删除 | |
| 5-2 | AdvancedSettings.tsx:22-26,37-38 | 终端后端/服务等级/工具集保存后不生效 | 前端写 `terminal.backend`≠后端 `env_type`（lib.rs:974）；`agent.service_tier/toolsets` 字段不存在 | ✅ 已验证 |
| 5-3 | FallbackSettings.tsx:145-153; DelegationSettings.tsx:201-209 | 禁用 fallback/delegation 清空不生效 | 禁用分支省略 key≠删除 + 悬空引用 | |
| 5-4 | SettingsPanel.tsx:254-272 | 导出配置泄露 remote_token 明文 | 导出未脱敏（RPC 层已脱敏但文件导出绕过） | |

### P2（2 项）
- ProviderCard.tsx:219-232 confirmDelete 乐观更新无回滚（删除失败池消失）
- ModelPickerPanel.tsx:110-114 selectedModel 跨 profile 陈旧

### Phase P 兼容性 ✅
ProfilePanel/ProviderCard/useModels/settings-store 均兼容 Phase P（per-profile 只读 model.ref，无 provider 硬耦合）。Phase P 时仅需改 useModels 数据源 + 新增全局 Provider 管理面板。

---

## Phase 6: Kanban 与辅助面板

### P1（2 项）
| # | 文件:行 | 问题 | 核查 |
|---|---------|------|------|
| 6-1 | api.ts:475 getApiBase()（裸 base）vs bridge.ts:308-312（手动注入 /p/） | HTTP profile 前缀不一致：Learning/Gateway/Usage/Rollback/Process 用裸 base 无 /p/，多 Profile 下显示错 profile 数据 | ✅ 已验证 |
| 6-2 | KanbanPanel.tsx:1260-2622（主1362L）+ 657-1229（TaskDrawer 573L） | 上帝文件 2622 行（40 useState/10 useEffect/18 useCallback），维护性严重劣化 | |

### P2（7 项）
- monitorState 死 prop（KanbanWindowApp:49 `<KanbanPanel/>` 不传 props → WS delegateTasks 合并路径死代码）
- staleConfig 模块级可变单例（KanbanPanel:213 let，effect 内重赋值不触发 re-render）
- DebugPanel 宫格模式数据冻结（useMessageStream enabled:viewMode==='single'）
- DebugPanel gatewayOnline 断线（App→SidePanel 透传未含 gatewayOnline，恒 false）
- SSE 事件处理逻辑重复（applyEvents vs 内联 handler ~30 行 copy-paste）
- SSE loadBoard 风暴（burst 事件每个 setTimeout loadBoard，无防抖）
- kanban-window 文档/行为矛盾（注释称"聚焦不重复创建"，实际 existing.close() toggle 关闭）

### KanbanPanel 拆分建议（按职责块）
- types/kanban.ts ← L101-189；constants.ts ← L195-207；kanban-helpers.ts ← L225-344
- TaskDrawer.tsx ← L657-1229（最高优先级，已自含）；TaskCard/KanbanColumn/AddLinkForm/primitives
- useKanbanSSE.ts ← L1388-1489；useKanbanBoard.ts ← L1262-1349
- ~10 个内联 modal 抽独立组件（CreateTaskDrawer/Dispatch/Reassign/CreateBoard 等）
- 预期：主组件 1362L → ~300-400L

### 辅助面板 verdict
Memory/Skills/Cron/Channels/FileBrowser/ProjectTree/Outline = WS RPC profile-aware ✅；Learning/Gateway/Usage/Rollback/Process = HTTP 裸 base（6-1）；Debug = 纯 props（宫格冻结 P2）；Tools = HTTP 半手动 ?profile。

---

## Phase 7: 语音与主题

### P2（2 项）
- GridModeView.tsx:78-81 Agent 状态 ring/bg 硬编码浅色 rgba（深色主题圆点变色光晕仍浅蓝，视觉错位）
- ThemePanel.tsx:94-101 toHex 对 color-mix/rgba 一律回退 #888888（颜色编辑器失真，自定义所见非所得）

**亮点**：useVoice 状态机干净（计时器/WS 订阅/闭包过期防护/错误回退全到位，UI 禁用态）；主题系统是全项目质量最高部分（presets→applyThemeCSS→style.css 三层派生 + 持久化三写 + boot 防闪烁）。

---

## Phase 8: 基础设施与错误处理

### P2（3 项）
| # | 文件:行 | 问题 |
|---|---------|------|
| 8-1 | ErrorBoundary 覆盖：App.tsx:946-1123 内层只包 PaneShell | overlays(Settings/Theme/About/ModelPicker)/CommandCenter/Toast/StatusBar 在内层边界外，任一崩溃 → 根边界接管 → 整个 App 全屏错误页（非优雅降级） |
| 8-2 | storage.ts:111-127,177 | 静默持久化失败：_persist/_updateIndex/saveBeacon fire-and-forget 仅 console.warn，配额/磁盘满时重启丢数据用户无感知 |
| 8-3 | storage.ts:62-66,104 | 迁移路径毒化 init：迁移检查在 try/catch 外，隐私模式/配额超限抛错 → _initPromise 未重置 → 永久毒化 → 会话恢复永久阻塞 |

### P3（多项）
- toast 非 keyed 无内容去重（重复错误堆 4 条）；slice(0,4) 截断孤儿定时器
- crypto.ts result 无空值校验；deepseek-webview 无显式 destroy/toggle 无互斥
- OverlayView 滚动锁非多实例安全（App 互斥已规避）；ErrorBoundary handleReload 无状态重置（潜在错误循环）

**亮点**：toast 去重/超时/上限/清理正确；storage init 重试是亮点；crypto 后端委托架构正确；deepseek-webview 防御性良好。

---

## 修复优先级建议

### Tier 1 — 快赢（高价值低风险，建议立即修）
- 5-2 AdvancedSettings 字段错配（改 env_type，删 service_tier/toolsets）
- 5-4 导出脱敏 remote_token
- 4-4 PreviewPanel sandbox 收紧 + URL 协议过滤（安全）
- 3-1 App 根订阅改派生（消灭 30fps 根重渲染，性能根因）
- 3-2/3-3 内联箭头→useCallback + ModelProvider value useMemo（trivial）
- 7-1 GridModeView 光晕主题化（trivial）
- 8-1 ErrorBoundary 包 overlays/CommandCenter

### Tier 2 — 中等（需设计）
- 5-1/5-3 config 删除/清空语义（merge_values 设计）
- 6-1 api.ts HTTP profile 前缀系统化
- 3-4/3-5 InputArea memo + 宫格虚拟化
- 8-2/8-3 storage 韧性（写失败上报 + 迁移保护）
- 7-2 ThemePanel toHex 颜色解析

### Tier 3 — 重构/功能补全（大）
- 6-2 KanbanPanel 拆分
- 4-1/4-2/4-5 终端多实例隔离 + agent 镜像链路补全（半建功能）
