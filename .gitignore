node_modules/
dist/
src-tauri/target/
*.log
.DS_Store
Thumbs.db

# Sidecar binaries (too large for git, built separately)
src-tauri/binaries/*.exe
*.bak-*
src-slint/target/
eleve-chat-desktop-new/
node_modules/
.ast-cache/
.tree-sitter-cache/
dist/
src/App.tsx.bak

# 🔴 画布插件 dist 打包源（build_release.ps1 生成，~36MB 含 wasm）——不入库
src-tauri/plugins/

# TypeScript 增量编译缓存
tsconfig.tsbuildinfo

# 🔴 运行时目录（附件 staging / 配对数据）——不提交
src-tauri/.eleve/

# 🔴 注意：src-tauri/permissions/autogenerated/ 必须入库！
# Tauri v2 ACL 权限定义（allow-*/deny-* TOML）——缺失时 build.rs 校验失败、
# 命令被 ACL 拒绝（2026-08-19 画布按钮 not allowed by ACL 事故根因）
PortableGit-*.7z.exe
